{
  "name": "payid-attestation",
  "version": "0.1.4",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "tsup src/verify.ts --format esm --dts --clean"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
