// RPC URLs — edit these when your dev node / fork changes
// 0G fork RPC (chain 16601) - points to your local fork via Vite proxy

export const RPC_URLS = {
  devNode: '/rpc',
  zeroG: '/rpc',
} as const;
